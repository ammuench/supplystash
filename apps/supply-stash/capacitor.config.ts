import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "ch.alexmuen.supplystash",
  appName: "Supply Stash",
  webDir: ".output/public",
};

export default config;
